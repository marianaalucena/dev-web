function Meta(descricao, dataInicio, dataFim, tipo, prioridade) {
  return {
    descricao: descricao,
    dataInicio: dataInicio,
    dataFim: dataFim,
    tipo: tipo,
    prioridade: prioridade,
  };
}

export default Meta();
